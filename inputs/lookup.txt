# SCRATCH. One search term per line; surnames work best.
#
# WRITTEN BY: you, or the workflow's `lookup` input.
# READ BY:    src/find_slug.py.  Results land in reports/lookup.md.
# Nothing else reads this and nothing depends on it — it is a lookup
# tool, not state.
abde
